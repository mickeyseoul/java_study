▶자바 API Application Programming Interface
프로그램 개발에 자주 사용되는 클래스 및 인터페이스 모음 
라이브러리
자바 API 도큐먼트 https://docs.oracle.com/en/java/javase/11/docs/api/


▶java.lang 패키지: 가장 기본적인   *임포트 필요없음

▷Object 클래스: 모든 클래스를 상속함
.toString(): 참조변수의 객체 번지 리턴/ 클래스에 자주쓰는 문자열 오버라이딩 해놓아서 편하게 String타입 리턴 가능
.equals(String anotherString): 문자열이 서로 같은지 boolean타입 리턴


▷String 클래스 ★
String str1 = new String("Nice day"); //100번지
String str2 = new String("Nice day"); //200번지
System.out.println(str1==str2); //주소비교 => false

String s = str.toString()  //Object 클래스 메서드에 오버라이딩되어 문자열 리턴
int i = str.length()

boolean bl = str.equals(str2); //object타입 상속받음
boolean bl = str.equalsIgnoreCase(str2)  //대소문자 구분없이 비교
int i = str.compareTo(str2); //같으면==0 다르면 !=0

str.toUpperCase() 대문자로 바꿔줌
str.toLowerCase() 소문자로 바꿔줌

str.subString(int beginIndex, int endIndex) :beginIndex부터 endIndex전까지 문자열 리턴 beginIndex<= s <endIndex
str.subString(int beginIndex)  		 :beginIndex부터 문자열 리턴

	ex)
	String fullName = "Hello.java";
	String fileName;
	String ext;
		
	int index = fullName.indexOf('.'); //5
	fileName = fullName.substring(0, index); //index전까지
	ext = fullName.substring(index+1);
		
	System.out.println("fileName:"+fileName);
	System.out.println("ext:"+ext);

boolean bl = str.contains("de");  :찾는 문자 존재하는지 boolean 리턴
int i = str.indexOf("de");  	  :찾는 문자 index번호로 알려줌 // 없으면 -1 리턴

str.replace(CharSequence target, CharSequence replacement)  target을 replacement로 전부 바꿔줌


▷Wrapper 클래스: 기본데이터 타입을 객체로 만들어야 하는 경우에 사용
Byte
Short
Integer
Long
Float
Double
Char
Boolean

Integer it = 10;  //autoboxing
int i = new Integer(10);  //unboxing

int i = Integer.parseInt("10");  문자열을 정수로 리턴
Integer it = Integer.valueOf("10");


▷Math 클래스
int i = (int)(Math.random()*41)+5;  5~45사이의 난수를 리턴 / (45-5+1)+5


▶java.util 패키지: 조미료 역할

▷Stringtokenizer  구분자
Stringtokenizer str = new Stringtokenizer("12/34 56,78:90","/ ,"); //구분자: 슬래쉬,띄어쓰기,콤마 //기본형: 띄어쓰기
int cnt = stn.countTokens(); //4

while(stn.hasMoreTokens()) { //boolean타입
	String s = stn.nextToken(); // 항상 짝으로 사용 hasMoreToken() - nextToken()
	System.out.println("s:"+s);
}

비교)String 클래스의 split() ★
String str = "12/34 56,78:90";
String[] arr = str.split("/"); //구분자 1개만 가능
int cnt = arr.length;

for(int i=0;i<arr.length;i++) {
	System.out.println(arr[i]);
}


▷Calendar
Calendar cal = Calendar.getInstance();  //static getInstance()를 통해 객체 생성 //현재시간 참조해야 하기 때문

cal.get(Calendar.YEAR)  // get()를 통해 호출할때 사용한 매개값은 모두 Calendar 클래스에 선언되어있는 상수
cal.get(Calendar.MONTH) //0=1월
cal.get(Calendar.DATE)
int hour = cal.get(Calendar.HOUR)
int min = cal.get(Calendar.MINUTE)
int sec = cal.get(Calendar.SECOND)

String[] week = {"","일","월","화","수","목","금","토","일"};
System.out.println(week[cal.get(Calendar.DAY_OF_WEEK)]+"요일");  //일요일이 1


▶Date 클래스와 SimpleDateFormat 클래스(java.text 패키지)
Date now = new Date();
System.out.println(now); //오버라이딩된 toString() 현재 날짜와 시간 정보 리턴

SimpleDateFormat sdf1 = new SimpleDateFormat("YY-MM-d");
System.out.println(sdf1.format(now));
		
SimpleDateFormat sdf2 = new SimpleDateFormat("yyyy/MM/dd hh:mm:ss a"); //"yyyy년MM월dd일 E요일 hh:mm:ss"
System.out.println(sdf2.format(now));



